<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NueApi - Free REST API for Developers</title>
    <link rel="icon" type="image/png" href="https://telegra.ph/file/e07e0fddc2a2967bd08d6.jpg">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #18bc9c;
            --bg-color: #f5f5f5;
            --text-color: #333;
            --link-color: #18bc9c;
            --link-hover-color: #16a085;
            --content-color: #666;
            --footer-bg-color: #2c3e50;
            --footer-text-color: #f5f5f5;
            --banner-bg: url('https://anime-reactions.uzairashraf.dev/sad/aqua-sad.jpg') no-repeat center center/cover; 
            --card-bg-color: #ffffff;
            --card-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        body.dark-mode {
            --bg-color: #333;
            --text-color: #f5f5f5;
            --card-bg-color: #444;
            --content-color: #ddd;
            --footer-bg-color: #222;
            --footer-text-color: #f5f5f5;
        }

        body {
            background-color: var(--bg-color);
            color: var(--text-color);
            font-family: 'Helvetica Neue', Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        /* Banner Section */
        .banner {
            background: var(--banner-bg);
            color: #fff;
            padding: 120px 20px;
            text-align: center;
            position: relative;
            overflow: hidden;
            transition: background-image 0.5s ease-in-out; /* Added transition for smooth image change */
        }

        .banner h1 {
            font-size: 3rem;
            margin-bottom: 15px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .banner p {
            font-size: 1.2rem;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
        }

        .banner-controls {
            position: absolute;
            top: 50%;
            width: 100%;
            display: flex;
            justify-content: space-between;
            transform: translateY(-50%);
        }

        .banner-controls i {
            cursor: pointer;
            font-size: 1.8rem;
            color: #fff;
            padding: 10px 20px;
            background-color: rgba(0, 0, 0, 0.3);
            border-radius: 5px;
            transition: background-color 0.3s;
        }

        .banner-controls i:hover {
            background-color: rgba(0, 0, 0, 0.5);
        }

        /* Menu Card */
        .menu-card {
            background-color: var(--card-bg-color);
            border-radius: 8px;
            box-shadow: var(--card-shadow);
            margin: -40px auto 20px auto;
            max-width: 80%;
            padding: 15px;
            position: relative;
            z-index: 1;
        }

        .menu-card .menu-item {
            display: block; /* Changed to block for better button layout */
            margin: 8px 0;
            text-align: center; /* Center buttons */
        }

        .menu-card .menu-item a {
            color: #fff; /* White text color for buttons */
            font-size: 1.1rem;
            padding: 10px 15px; /* Adjusted padding */
            text-decoration: none;
            background-color: var(--link-color); /* Background color for buttons */
            border-radius: 5px;
            transition: background-color 0.3s;
            display: inline-block; /* Display as inline-block for better spacing */
        }

        .menu-card .menu-item a i {
            margin-right: 8px;
        }

        .menu-card .menu-item a:hover {
            background-color: var(--link-hover-color);
        }

        /* Theme Toggle */
        .toggle-theme {
            cursor: pointer;
            color: var(--link-color);
            font-size: 1.4rem;
            text-align: center;
            padding: 8px;
            transition: color 0.3s;
            position: fixed;
            top: 10px;
            right: 10px;
            z-index: 10;
        }

        .toggle-theme:hover {
            color: var(--link-hover-color);
        }

        /* Content Sections */
        .content, .blog, .json-display {
            padding: 30px 20px;
            text-align: center;
        }

        .content h2, .blog h2, .json-display h2 {
            font-size: 1.8rem;
            color: var(--primary-color);
            margin-bottom: 15px;
        }

        .content p, .blog p, .json-display p {
            color: var(--content-color);
            font-size: 1rem;
            margin-bottom: 15px;
        }

        /* Cards */
        .card {
            box-shadow: var(--card-shadow);
            border-radius: 8px;
            border: none;
            margin-bottom: 15px;
        }

        .card-header {
            background-color: var(--primary-color);
            color: white;
            font-size: 1.3em;
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
        }

        .card-body {
            background: var(--card-bg-color);
            border-bottom-left-radius: 8px;
            border-bottom-right-radius: 8px;
            padding: 15px;
        }

        /* Table Styling */
        .table {
            margin-bottom: 0;
        }

        .table thead th {
            border-bottom: 2px solid #dee2e6;
        }

        .table tbody tr:hover {
            background-color: #f1f1f1;
        }

        .table td, .table th {
            padding: 0.8em;
        }

        /* Footer */
        footer {
            background-color: var(--footer-bg-color);
            color: var(--footer-text-color);
            padding: 15px 0;
            text-align: center;
        }

        .footer-links a {
            color: var(--footer-text-color);
            margin: 0 8px;
        }

        .footer-links a:hover {
            color: var(--link-hover-color);
        }
    </style>
</head>
<body>

   <a href='https://www.highratecpm.com/k2uy5c8d?key=7457990c76a2adeb9a44f94168f6a792'> <div class="banner">
    <div class="banner-controls">
            <i class="fas fa-chevron-left" onclick="prevImage()"></i>
            <i class="fas fa-chevron-right" onclick="nextImage()"></i>
        </div>
        <h1>Welcome to NueApi</h1>
        <p>Explore our API and discover amazing features!</p>
    </div> </a>
    
    <div class="menu-card">
        <div class="menu-item"><a href="/menu"><i class="fas fa-ellipsis-h"></i> List Endpoint</a></div>
        <div class="menu-item"><a href="https://whatsapp.com/channel/0029Vaj5ZZg5kg7EYRniXS1Z"><i class="fas fa-circle"></i> Info Update</a></div>
    </div>
<script type="text/javascript">
        atOptions = {
            'key' : '2bc0c6c2f3b062ed165d140adef0629d',
            'format' : 'iframe',
            'height' : 60,
            'width' : 468,
            'params' : {}
        };
    </script>
    <script type="text/javascript" src="//www.topcreativeformat.com/2bc0c6c2f3b062ed165d140adef0629d/invoke.js"></script>
    <div class="toggle-theme" onclick="toggleTheme()"><i class="fas fa-adjust"></i></div>

    <div class="container"> 
        <div class="row"> 
            <div class="col-md-8 offset-md-2"> 
                <div class="blog">
                    <div class="card">
                        <div class="card-header text-center">
                            About This Website
                        </div>
                        <div class="card-body">
                            <p>🎉 <b>100% Free REST API</b> 🎉</p>
                            <p>🚀 With awesome features like:</p>
                            <ul>
                                <li>🤖 <b>Artificial Intelligence</b></li>
                                <li>📥 <b>Downloader</b></li>
                                <li>🔍 <b>Search</b></li>
                                <li>🔧 <b>Tools for Web/Bot Development</b></li>
                            </ul>
                            <p>This API uses <b>GET</b> requests over HTTPS. It's developed and maintained by a single person, so please be patient with any errors or updates.</p>
                            <p>Enjoy using the API! ✨</p>
                        </div>
                    </div>
                </div>

                <div class="json-display">
                    <div class="card">
                        <div class="card-header text-center">
                            Request API NueApi
                        </div>
                        <div class="card-body">
                            <table class="table table-hover">
                                <thead>
                                    <tr>
                                        <th>Key</th>
                                        <th>Value</th>
                                    </tr>
                                </thead>
                                <tbody>
                                    <% for(let key in jsonData) { %>
                                        <tr>
                                            <td><%= key %></td>
                                            <td><%= jsonData[key] %></td>
                                        </tr>
                                    <% } %>
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div> 
        </div> 
    </div> 

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <script>
        const images = [
            "/api/anime-reaction",
            "/api/anime-reaction?no=1",
            "/api/anime-reaction?no=2",
            "/api/anime-reaction?no=3",
            "/api/anime-reaction?no=4",
            "/api/anime-reaction?no=5",
        ];
        let currentIndex = 0;

        function updateBanner() {
            document.querySelector('.banner').style.backgroundImage = `url(${images[currentIndex]})`;
        }

        function nextImage() {
            currentIndex = (currentIndex + 1) % images.length;
            updateBanner();
        }

        function prevImage() {
            currentIndex = (currentIndex - 1 + images.length) % images.length;
            updateBanner();
        }

        setInterval(nextImage, 10000); 

        function toggleTheme() {
            document.body.classList.toggle('dark-mode');
            localStorage.setItem('theme', document.body.classList.contains('dark-mode') ? 'dark' : 'light');
        }

        document.addEventListener('DOMContentLoaded', () => {
            if (localStorage.getItem('theme') === 'dark') {
                document.body.classList.add('dark-mode');
            }
            updateBanner();
        });
    </script>

    <script type="text/javascript" src="https://www.freevisitorcounters.com/en/home/counter/1146079/t/1"></script>
    <script type='text/javascript' src='https://cdn.trakteer.id/js/embed/trbtn.min.js?date=18-11-2023'></script>
    <script type='text/javascript'>
        (function(){
            var trbtnId = trbtn.init('Belikan saya Rokok🙃','#be1e2d','https://trakteer.id/NueApi/tip','https://trakteer-uploads.sgp1.digitaloceanspaces.com/images/units/uic-LzjGUfjMrIvdzKmZJprHAfIQBkQ9uJEI1721682244.jpeg','40');
            trbtn.draw(trbtnId);
        })();
    </script>
    <script src="https://static.cbox.ws/embed/2.js" async></script>
    <script>
        window['CboxReady'] = function (Cbox) {
            Cbox('button', '5-951321-TFAjW9');
        }
    </script>

<footer>
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <div class="footer-links">
                        <a href="https://hammerhead-foamy-tune.glitch.me/">NueAi</a>
                        <a href="https://wa.me/6283894391287">Contact Site Creator</a>
                    </div>
                    <p>© 2024 NueApi. All rights reserved.</p>
                </div>
            </div>
        </div>
</footer>
</body>
</html>